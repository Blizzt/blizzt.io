// Dependencies
import React from 'react';

// Styled Components
import {
  Layout
} from './styles';

const FormErrorText = ({ text }) => (
	<Layout>{text}</Layout>
);

export default FormErrorText;
